# archive/

Loose design notes and ad-hoc dumps relocated from the repo root for hygiene (P1).
Historical/working documents, not authoritative specs. Canonical docs live in
`docs/`; structured root docs stay at the root (ARCHITECTURE.md, API_SPEC.md,
BUILD_PLAN.md, REQUIREMENTS.md, TEST_PLAN.md, TODO.md, AGENTS.md,
AUTONOMOUS_PERMISSIONS.md).

Nothing here is referenced by code, CI, or the mkdocs site (docs_dir=docs, so this
folder is outside the published site). Kept in-tree to preserve history.
